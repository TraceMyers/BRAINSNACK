#include "object_grid.h"
#include "../globals.h"

void ObjectGrid::Init()
{
    // CollisionChannelOccupancy.Init(OBJECT_GRID_CELL_COUNT, &Session.RuntimeAllocator);
    // ObjectOccupancy.Init(OBJECT_GRID_CELL_COUNT, &Session.RuntimeAllocator);
    // const float32 GridSideLength = OBJECT_GRID_SIDELENGTH_IN_CELLS;
    // UpperLeftOrigin = TVector2(-GridSideLength, -GridSideLength) * 0.5f;
}

s32 ObjectGrid::WorldToGrid1d(TVector2 Position)
{

}

s32 ObjectGrid::WorldToGrid2d(TVector2 Position)
{
}

s32 ObjectGrid::Grid2dToWorld(TVector2 Position)
{
}
